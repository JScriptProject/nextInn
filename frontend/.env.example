# Copy to .env for local dev, or set in Vercel project settings for production.
# Vite bakes VITE_* vars in at build time — redeploy after changing on Vercel.

VITE_API_URL=http://localhost:8000

# Production (Vercel):
# VITE_API_URL=https://nextinn.onrender.com
